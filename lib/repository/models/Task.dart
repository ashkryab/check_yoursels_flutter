class Task {
  int id, maxCount, currentCount;
  String title, description;
  String lastUpdate;

  Task({this.id, this.maxCount=0, this.currentCount = 0, this.title, this.description, this.lastUpdate});
}
